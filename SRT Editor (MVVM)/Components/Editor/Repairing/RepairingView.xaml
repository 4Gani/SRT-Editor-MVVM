<UserControl x:Class="SRTEditor_MVVM.Components.Editor.Repairing.RepairingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SRTEditor_MVVM.Components.Editor.Repairing"
             mc:Ignorable="d" 
             d:DesignHeight="290" d:DesignWidth="684">

    <Canvas>
        <Grid Background="{StaticResource appDisableBackground}"
              Width="684" Height="5" VerticalAlignment="Top"/>
        <Grid Background="{StaticResource appColorBackground}"
              Width="172" Height="5" VerticalAlignment="Top"/>
        
        <TextBlock Width="564" Height="50"
                   TextWrapping="Wrap" TextAlignment="Center"
                   Canvas.Left="60" Canvas.Top="45">
            Repairing fixes the structure and ordering of lines in the SRT file
        </TextBlock>
        
        <Button Content="Repair" Command="{Binding RepairCommand}"
                Style="{StaticResource normalButton}"
                Canvas.Left="292" Canvas.Top="150"/>
    </Canvas>
</UserControl>
